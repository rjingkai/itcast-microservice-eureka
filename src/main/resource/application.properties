server.port=6868


#是否将自己注册到注册中心中，由于这里就是服务中心，所以写false
eureka.client.register-with-eureka=false


#是否从服务中心获取信息
eureka.client.fetch-registry=false

eureka.client.service-url.defaultZone=http://127.0.0.1:${server.port}/eureka/

